# Color Tokens

- `background` — define semantic value in the implementation token file.
- `surface` — define semantic value in the implementation token file.
- `text` — define semantic value in the implementation token file.
- `muted` — define semantic value in the implementation token file.
- `border` — define semantic value in the implementation token file.
- `success` — define semantic value in the implementation token file.
- `warning` — define semantic value in the implementation token file.
- `danger` — define semantic value in the implementation token file.
- `info` — define semantic value in the implementation token file.
