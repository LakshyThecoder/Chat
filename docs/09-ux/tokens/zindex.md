# Zindex Tokens

- `base` — define semantic value in the implementation token file.
- `sticky` — define semantic value in the implementation token file.
- `overlay` — define semantic value in the implementation token file.
- `modal` — define semantic value in the implementation token file.
- `toast` — define semantic value in the implementation token file.
