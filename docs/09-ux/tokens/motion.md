# Motion Tokens

- `duration-fast` — define semantic value in the implementation token file.
- `duration-base` — define semantic value in the implementation token file.
- `duration-slow` — define semantic value in the implementation token file.
- `easing-standard` — define semantic value in the implementation token file.
