# Radius Tokens

- `sm` — define semantic value in the implementation token file.
- `md` — define semantic value in the implementation token file.
- `lg` — define semantic value in the implementation token file.
- `xl` — define semantic value in the implementation token file.
- `pill` — define semantic value in the implementation token file.
