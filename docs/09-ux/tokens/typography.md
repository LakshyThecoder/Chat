# Typography Tokens

- `font-family` — define semantic value in the implementation token file.
- `font-size` — define semantic value in the implementation token file.
- `line-height` — define semantic value in the implementation token file.
- `font-weight` — define semantic value in the implementation token file.
- `letter-spacing` — define semantic value in the implementation token file.
