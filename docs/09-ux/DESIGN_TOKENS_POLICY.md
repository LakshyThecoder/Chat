# Design Token Policy

All visual values should resolve through semantic tokens.
No one-off colors/spacing/radii in feature code unless explicitly documented as an exception.

Component-level tokens may compose primitives but should not create arbitrary parallel systems.
