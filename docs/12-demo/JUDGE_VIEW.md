# Judge View

The product should make these visible without reading code:

- real consumer problem (cancelled flight, billed-after-cancel, already-claimed booking)
- WebMCP tools registered on this URL
- live provider row painted by `inspect_counter`
- deterministic entitlement (engine amount)
- permission boundary (`APPROVAL_REQUIRED` until human signs)
- real provider mutation after signature
- verification as expected vs observed
- blocked duplicate filing on FR0999 / BERG

Canonical surface: `/` (Resolution Theater). The Bureau inbox/docket is not the judge path.
