# Demo Check
Validate the competition path:
input → understanding → evidence → strategy → permission → WebMCP tool → provider state change → verification → monitoring.
Check that every visible success is backed by actual state.
